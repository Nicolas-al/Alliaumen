{% extends 'base.html.twig' %}

{% block body %}
<div class="block" id="block_portfolio">
    <div id="title_projects">
        <h2>Projets Réalisés</h2>
    </div>
    <div class="row mb-2">
        <div class="col-lg-8 offset-lg-3">                            
        <p class="text-white text-center"> Ici sont présentés les projets que j'ai pu effectuer. Constatez par vous même en vous rendant directement sur un projet. N'hésitez pas à selectionner une carte projet pour afficher son contenu sur le panneau. En bas du panneau sont détaillées les compétences utilisés</p>
        </div>
    </div>
    <div class="row position-relative" id="project_gallery">
            {% for project in projects %}
            <div class="panel_project">
                
                <div class="project">
                    <img src="{{ vich_uploader_asset(project, 'imageFile') | imagine_filter('slider_thumb')}}" alt="image projet">
                </div>
                {# <div class="project_link">
                        <a href="{{ project.link }}" class=""> voir le site </a>
                </div>
                <p> {{ project.description }}</p>                           
                <div class="opacity-description">
                </div> #}
            </div>

            {% endfor %}
        </div>
</div>          

{% endblock %}
